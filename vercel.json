{
  "version": 2,
  "installCommand": "npm install -g corepack && corepack enable && pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm approve-builds sharp unrs-resolver && pnpm run build"
}
